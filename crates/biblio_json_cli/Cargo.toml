[package]
name = "biblio_json_cli"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.5.46", features = ["derive"] }
biblio_json = { path = "../biblio_json" }
itertools = "0.14.0"
